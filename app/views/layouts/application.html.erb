<!DOCTYPE html>
<html lang="en">
  <head>
    <title>BlogIt</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="bg-gray-100 font-sans leading-normal tracking-normal flex flex-col min-h-screen w-full">
    <!-- Header -->
    <header class="bg-blue-600 p-4 text-white">
      <h1 class="text-2xl">BlogIt</h1>
    </header>
    <div class="flex-1 container mx-auto">
      <!-- Alerts -->
      <div class="p-4">
        <%= render "shared/alerts" %>
      </div>
      <!-- Main Content -->
      <main class="p-4  max-w-[48.1rem] m-auto">
        <%= yield %>
      </main>
    </div>
    <!-- Footer -->
    <footer class="bg-blue-600 p-4 text-white mt-auto">
      <p>&copy; 2023 BlogIt. All rights reserved.</p>
    </footer>
  </body>
</html>
